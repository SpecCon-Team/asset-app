# Environment Configuration
NODE_ENV="development"

# Database Configuration
# -------------------------------------------------
# The app automatically switches between databases based on NODE_ENV:
# - development: Uses LOCAL_DATABASE_URL (Docker)
# - production: Uses NEON_DATABASE_URL (Neon Cloud)

# Local Docker Database (Development)
# Default credentials from docker-compose.yml
LOCAL_DATABASE_URL="postgresql://postgres:postgres@localhost:5433/asset_app"

# Neon Cloud Database (Production)
# Get your connection string from: https://console.neon.tech
# Format: postgresql://[user]:[password]@[endpoint].neon.tech/[database]?sslmode=require
NEON_DATABASE_URL="postgresql://user:password@ep-xxx-xxx.region.neon.tech/database?sslmode=require"

# Dual-Write Mode (Automatic Synchronization)
# When enabled, all write operations are sent to BOTH databases automatically
# - Set to "true" to enable automatic sync
# - Set to "false" to use only the primary database
ENABLE_DUAL_WRITE="true"

# Fallback Database URL (if specific URLs not set)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/asset_app"

# -------------------------------------------------
# Application Configuration
# IMPORTANT: Generate a strong random JWT secret (64+ characters)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="REPLACE_WITH_STRONG_RANDOM_SECRET_MINIMUM_64_CHARACTERS"
PORT=4000
CLIENT_URL="http://localhost:5173"

# -------------------------------------------------
# Email Configuration (for password reset emails)
# Gmail example - you need to create an App Password
# Go to: https://myaccount.google.com/apppasswords
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_SECURE="false"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-password"

# -------------------------------------------------
# WhatsApp Business API Configuration
# Get credentials from: https://developers.facebook.com
# 1. Create a Meta Business Account
# 2. Set up WhatsApp Business API in your app
# 3. Get your Phone Number ID and Access Token
WHATSAPP_PHONE_NUMBER_ID="your_phone_number_id"
WHATSAPP_ACCESS_TOKEN="your_permanent_access_token"
WHATSAPP_BUSINESS_ACCOUNT_ID="your_business_account_id"
WHATSAPP_API_VERSION="v21.0"
# Webhook verify token (you create this - can be any secure random string)
WHATSAPP_VERIFY_TOKEN="my_secure_verify_token_12345"

# -------------------------------------------------
# How to use:
# 1. Copy this file to .env
# 2. Replace NEON_DATABASE_URL with your actual Neon connection string
# 3. Change JWT_SECRET to a secure random string
# 4. Set NODE_ENV="production" when deploying to production
# 5. Configure email settings with your email provider credentials
# 6. Add your WhatsApp Business API credentials
